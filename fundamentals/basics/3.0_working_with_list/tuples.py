# tuples = immutable list
dimensions = (200, 50)
print(dimensions[0])
print(dimensions[1])

# looping through all values
for dimension in dimensions:
    print(dimension)

# writing over a tuple
for dimension in dimensions:
    print(dimension)

dimensions = (400, 100)
for dimension in dimensions:
    print(dimension)